{
  "name": "@root/elysia-api",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build:backend": "bash scripts/build-backend.sh",
    "prebuild": "yarn build:backend",
    "build": "yakumo build",
    "bump": "yakumo version",
    "dep": "yakumo upgrade",
    "pub": "yakumo publish",
    "lint": "yarn eslint packages --cache --ext=ts",
    "lint-fix": "yarn eslint packages --cache --ext=ts --fix"
  },
  "devDependencies": {
    "atsc": "^2.1.0",
    "eslint": "^8.57.1",
    "koishi": "^4.18.9",
    "typescript": "^5.9.2",
    "yakumo": "^1.0.0",
    "yakumo-esbuild": "^1.0.0",
    "yakumo-tsc": "^1.0.0"
  }
}
